# 08. 자원·규정 감사 — 현재 설계(C안)가 통과하는가

감사일: 2026-09-07 · 대상: `02` §4 아키텍처 · 근거: 공고문 p.4·p.5·p.10, `01` 규정표

> **측정 한계**: 본 감사 환경에 GPU가 없어 **추론 시간은 전부 계산 기반 추정치**다. `05` R1에서 실측으로 대체해야 하며, 그 전까지 아래 시간 수치를 확정값으로 쓰지 않는다. 용량과 라이선스는 확정 사실이다.

---

## 0. 결론

**규정 위반은 없다. 그러나 반드시 고쳐야 할 문제가 두 개, 확인해야 할 것이 다섯 개 있다.**

| | 문제 | 심각도 |
|---|---|---|
| **F1** | **원본 해상도 VAE 왕복(A1)이 T4 VRAM을 넘길 수 있다** | **높음 — 설계 수정 필요** |
| **F2** | **FLUX.1-dev는 생성물까지 비상업 제한이라 제출물에 딸려온다** | **높음 — 자산 교체 필요** |
| F3 | SAM ViT-H는 용량·속도 모두 불가 → MobileSAM 고정 | 중간 (이미 반영됨) |
| F4 | CLIP ViT-L/14 하나가 856MB — 제출 용량의 대부분 | 중간 |
| F5 | B1 잎맥 그래프 추출이 CPU 병목이 될 수 있다 | 중간 |
| F6 | **제출 후에는 자원 초과를 이유로 브랜치를 뺄 수 없다** | **높음 — 절차 문제** |

---

## 1. Colab 자원 감사

공고문 p.4: *"일반적인 Google Colab 자원 범위에서 정상적으로 실행 가능하여야 하며, 실행환경의 자원 한계로 정상적인 평가가 어려운 경우 기술적 보완을 요청할 수 있음"*

무료 등급 **T4 (16GB VRAM, 시스템 RAM 12~13GB)** 를 하한으로 가정한다. 실제 평가환경 등급은 9/18 확인 항목이다.

### 1.1 F1 — VAE 왕복의 VRAM (가장 큰 위험)

**문제.** A1은 이미지를 VAE로 인코드→디코드해 재구성 오차를 본다. 그런데 우리 설계 원칙은 **리사이즈 금지**다. AI Hub 스마트팜 이미지는 1920×1080급이 흔하다.

VAE는 컨볼루션 스택이라 **activation 메모리가 해상도에 선형 비례**한다. 512² 기준으로 수백 MB면, 1920×1080은 그 약 8배다. 디코더 중간 레이어는 잠재의 8배 해상도 × 128~512 채널이라 여기서 수 GB가 잡힌다. **T4에서 OOM이 날 수 있고, 나지 않더라도 배치 크기가 1로 묶인다.**

**해결 — 타일 단위 왕복을 설계에 못박는다.**

```
원본 이미지
  → 겹침 있는 타일로 분할 (예: 512×512, overlap 64)
  → 타일마다 VAE 왕복 → 타일별 재구성 오차
  → 타일 오차를 top-k 풀링 (전역 평균 아님)
```

부수 효과가 두 개 있는데 둘 다 유리하다.
- **VRAM이 타일 크기로 고정된다** — 입력 해상도와 무관해진다
- **top-k 풀링이 유형 ①②에 유리하다** — 잠재 인페인팅으로 일부만 재생성된 이미지는 그 영역의 타일에서만 오차가 낮게 나온다. 전역 평균은 이 신호를 희석시킨다

> **이건 성능을 위해서도 원래 했어야 하는 것이다.** 자원 제약이 더 나은 설계를 강제한 경우다.

### 1.2 F3 — 분할 모델은 MobileSAM으로 고정

| | params | fp16 용량 | 이미지당 인코딩 (T4 추정) |
|---|---|---|---|
| SAM ViT-H | 636M | **1,272MB** | **~1초** |
| MobileSAM | 10M | 20MB | ~10ms |

SAM ViT-H는 **용량만으로 제출 한도를 위협**하고, 5,000장이면 인코딩만 80분 이상이다. **MobileSAM(Apache-2.0) 고정.** 품질이 부족하면 SAM2-tiny를 검토하되 용량을 먼저 잰다.

### 1.3 F4 — 제출 용량

| 구성 | 내역 | 합계 |
|---|---|---|
| **경량** | FerretNet 2MB + VAE 166MB + MobileSAM 20MB + DINOv2 ViT-B 172MB + A4 50MB + B1 60MB | **≈ 470MB** |
| 중량 | 위에서 SAM ViT-H(1,272MB) + CLIP ViT-L/14(856MB) 사용 | ≈ 2,400MB |

**CLIP ViT-L/14 하나가 856MB로 경량 구성 전체보다 크다.** A5 보조는 **DINOv2 ViT-B(172MB, Apache-2.0)** 로 간다. 성능 차이는 자체 검증으로 확인하되, 차이가 작으면 용량이 우선이다.

> 제출 총 용량 한도는 **9/18 확인 항목**이다. 확인 전까지 **경량 구성을 기본값**으로 두고 설계한다.

### 1.4 F5 — 축 B의 CPU 병목

B1(잎맥 그래프)은 GPU가 아니라 **CPU에서 도는 작업**이다 — 세선화(skeletonize) → 그래프 구축 → 네트워크 특징 계산. 순수 Python/scipy 구현이면 이미지당 0.5~2초가 나올 수 있다.

| 테스트 장수 | B1만의 소요 (추정) |
|---|---|
| 500장 | 4~17분 |
| 2,000장 | 17분~1.1시간 |
| 5,000장 | 42분~2.8시간 |

Colab 런타임 한도(무료 ~12시간, 유휴 90분 종료) 안이기는 하나 **다른 지표와 합치면 위험 구간에 들어간다.**

**대응**
- 잎맥 분석은 **마스크를 축소한 뒤** 수행한다 (여기서는 리사이즈가 해롭지 않다 — 저수준 흔적이 아니라 **위상 구조**를 보기 때문이다)
- 그래프 특징 계산을 벡터화하고, 이미지당 분석 잎 수에 상한을 둔다
- **B2(시설 구조물)는 OpenCV Hough·FFT라 훨씬 싸다.** 종 미확정 상태에서 B2를 먼저 만드는 결정이 자원 면에서도 옳다

### 1.5 추론 예산표 (틀만 확정, 숫자는 R1에서 채운다)

| 단계 | 장당 소요 | 500장 | 2,000장 | 5,000장 |
|---|---|---|---|---|
| A5 FerretNet | 실측 필요 | | | |
| A1 VAE 타일 왕복 | 실측 필요 | | | |
| A2 스펙트럼 (CPU) | 실측 필요 | | | |
| A4 영역 대조 (CPU) | 실측 필요 | | | |
| MobileSAM 분할 | 실측 필요 | | | |
| B1 잎맥 그래프 (CPU) | 실측 필요 | | | |
| B2 구조물 (CPU) | 실측 필요 | | | |
| **합계** | | | | |

**판정 기준**: 합계가 Colab 런타임 한도의 **50% 이내**여야 한다. 넘으면 제출 전에 지표를 뺀다(§3 F6).

---

## 1.6 학습 자원 — 슈퍼컴이 필요한가

**결론: 필요 없다. VRAM 16GB 한 장이면 전 과정이 돈다.** 다만 GPU 시간을 먹는 곳이 통념과 다르다.

### 1.6.1 우리 설계는 대부분 학습이 필요 없다

| 구성요소 | 학습 | 필요 자원 |
|---|---|---|
| A1 잠재-디코더 | **불요** — 사전학습 VAE를 그대로 왕복 | 0 |
| A2 스펙트럼 | **불요** — FFT + power-law 적합 | CPU |
| A3 양자화 | **불요** — 코드북 재양자화 오차 | CPU + 소량 GPU |
| **A4 영역 대조** | **필요** — 이중스트림 CNN (~25M) | GPU 2~4시간 (T4 기준) |
| **A5 전역 판별** | **필요** — FerretNet 1.06M 파인튜닝 | GPU 0.5~1시간 |
| A5 보조 (DINOv2) | **사실상 불요** — 백본 동결, 선형 분류기만 | 특징추출 ~20분 + 학습 수 분 |
| 분할 (MobileSAM) | **불요** — 사전학습 그대로 | 0 |
| B1 형태 정합성 | **불요** — 고전 ridge 필터 + 그래프 특징 → 밀도추정 | **CPU만** |
| B2 시설 구조물 | **불요** — Hough + FFT → 밀도추정 | **CPU만** |
| B3 생육 정합성 | **불요** — B1 산출물 재사용 | **CPU만** |
| 융합 (가중합) | LOGO CV 튜닝 | CPU, 초 단위 |

> **축 B 전체가 GPU를 쓰지 않는다.** 고전 영상처리 + 저차원 특징의 정상 분포 모델링(One-Class SVM / GMM / Isolation Forest)이라 학습이 CPU에서 수 분에 끝난다. 대신 **특징 추출 전처리가 CPU 시간을 먹으므로 코어 수가 중요하다.**

**학습만 놓고 보면 T4 한 장으로 반나절이면 끝난다.**

### 1.6.2 진짜 GPU를 먹는 곳은 학습이 아니라 **학습 데이터 생성**이다

운영측이 데이터를 주지 않으므로 가짜 이미지를 전량 우리가 만들어야 한다(`03`). 이쪽이 모델 학습보다 한 자릿수 무겁다.

**목표 생성량** (계열당 3,000~5,000장, 총 15,000~25,000장)

| 계열 | 방법 | 장당 소요 (추정) |
|---|---|---|
| G1 확산-잠재 | SD1.5 512² / SDXL 1024², 30스텝 | 4090 ~5초 · **T4 ~40초** |
| G2 플로우 매칭 | FLUX.1-schnell 4스텝 / SD3.5-medium | 4090 ~2~5초 · **T4 ~20초** |
| G3 GAN | 스타일 변환 | ⚠ §1.6.4 참조 |
| **G4 편집-픽셀공간** | 알파 · Poisson · 하모나이제이션 | **GPU 거의 불필요 (OpenCV)** |
| G5 편집-잠재공간 | 인페인팅 · img2img | G1과 동급 |

**총 소요 추정 (20,000장 기준)**

| GPU | 평균 장당 | 총 시간 |
|---|---|---|
| RTX 4090 / A100 (24~40GB) | ~5초 | **약 28시간 — 하루 반** |
| RTX 3090 / A5000 (24GB) | ~10초 | 약 2.5일 |
| **T4 (16GB, Colab 무료급)** | ~30초 | 약 7일 — 빠듯하지만 가능 |

온라인대회 기간이 **4주(9/21~10/18)** 이므로 어느 쪽이든 시간 안에 들어간다.

### 1.6.3 최소·권장 사양

| | 최소 | 권장 |
|---|---|---|
| **VRAM** | **16GB** (SDXL fp16 8~12GB, FLUX schnell 12~16GB, A4 학습 batch32 ~8GB) | 24GB |
| GPU | T4 / RTX 3060 12GB(양자화 시) | RTX 4090 · A5000 · A100 **1장** |
| CPU | 8코어 | 16코어+ (축 B 전처리·증강이 CPU 바운드) |
| RAM | 32GB | 64GB |
| **저장공간** | **500GB** | **1TB** |

> **저장공간이 은근한 병목이다.** 생성 20,000장(PNG 1024² ≈ 1.5MB) = 30GB에, AI Hub 스마트팜 원본이 수십~수백 GB다. `05` R1에서 GPU와 **함께** 확인할 것.

**GPU는 여러 장이 아니라 한 장이면 된다.** 우리 모델 중 가장 큰 것이 25M 파라미터라 분산 학습이 무의미하다. 다만 **데이터 생성은 완전 병렬화되므로**, 랩 서버에 GPU가 여러 장 있으면 생성 작업만 나눠 돌려 시간을 그 배수만큼 줄일 수 있다.

### 1.6.4 G3(GAN 계열)은 학습하지 말고 우회한다

CycleGAN을 직접 학습하면 수백 에폭이 필요해 **며칠**이 든다. 그런데 GAN은 2026년 텍스트→이미지에서 거의 퇴장했고(`06` §2), 우리 목적은 "GAN 흔적을 가진 샘플 확보"이지 좋은 도메인 변환기를 만드는 게 아니다.

**대안 (전부 학습 불요)**
- 사전학습 CycleGAN 공개 가중치를 그대로 추론에만 사용 (라이선스 확인)
- 고전 뉴럴 스타일 트랜스퍼 (Gatys 계열) — 사전학습 VGG만 있으면 됨
- **증류 모델(SDXL-Turbo, FLUX schnell)의 출력을 활용** — ADD 계열은 판별자를 쓰므로 GAN 흔적이 섞여 들어온다(`06` §7)

→ **G3에 GPU 학습 시간을 쓰지 않는다.** 비중도 낮게 잡는다.

### 1.6.5 GPU가 아예 없다면

Colab Pro의 컴퓨팅 단위로 **데이터 생성도 가능하다.** 단 두 가지를 지킨다.

- **세션 종료 대비 체크포인트** — 생성 스크립트를 재시작 가능하게 만들고, 이미 만든 장수를 파일 단위로 기록해 이어서 돌린다
- **생성물을 즉시 Drive에 저장** — 런타임이 끊기면 로컬 디스크가 날아간다

### 1.6.6 슈퍼컴을 쓸 때 오히려 조심할 것

랩 서버가 배치 스케줄러(SLURM 등) 기반이면 다음이 실무 문제가 된다.

- **큐 대기 시간** — 짧은 실험을 반복하는 우리 작업 패턴과 안 맞을 수 있다. 대화형 세션 할당이 가능한지 확인
- **외부 네트워크 차단** — 계산 노드에서 인터넷이 막혀 있으면 **모델 가중치를 미리 받아 두어야 한다.** FerretNet 공식 가중치가 Baidu Disk 배포라는 점(`01` §3)과 겹쳐 특히 주의
- **환경 격리** — conda/컨테이너 사용 가능 여부. `01` §6의 버전 고정 재현성 요구와 직결

> **결론: "슈퍼컴을 빌릴 수 있는가"보다 "GPU 1장을 4주간 자유롭게 쓸 수 있는가"가 중요하다.** `05` R1의 문의 문항을 이 기준으로 쓴다.

---

## 2. 규정 감사

### 2.1 F2 — FLUX.1-dev 라이선스 (반드시 교체)

확인된 사실: **FLUX.1-dev는 비상업 라이선스이고, 그 제한이 모델 가중치뿐 아니라 생성된 출력물과 파생 모델에까지 미친다.** 반면 **FLUX.1-schnell은 Apache-2.0**으로 제한이 없다.

본 대회에서 이게 두 군데를 건드린다.

**(가) 학습 데이터 생성용으로 dev를 쓸 때**
공고문 p.5는 *"실제 활용한 학습데이터"* 제출을 요구하고, p.10은 주최·주관기관의 **무상·비독점 이용권**을 규정한다. dev 출력물을 학습 데이터로 제출하면 그 비상업 제한이 제출물에 따라붙는다. p.10이 열거한 용도(운영·평가·검증·시상·교육·홍보·**비영리** 성과확산)는 비상업이라 정면 충돌은 아니지만, **명백한 회색지대다.**

**(나) A1이 쓰는 VAE 가중치 — 이쪽이 더 직접적이다**
A1은 VAE를 **제출물에 포함**해야 한다(p.5, 예외 없음). **FLUX.1-dev의 VAE를 쓰면 비상업 제한이 붙은 가중치를 제출하게 된다.**

**조치**

| 용도 | 쓸 것 | 피할 것 |
|---|---|---|
| A1의 VAE 가중치 | **FLUX.1-schnell VAE (Apache-2.0)** + SD1.5/SDXL VAE (OpenRAIL-M) | FLUX.1-dev VAE |
| 플로우 매칭 계열 학습 데이터 생성 | **FLUX.1-schnell (Apache-2.0)** | FLUX.1-dev |

**단, 트레이드오프가 있다.** schnell은 4스텝 증류 모델이라 full-step인 dev와 흔적이 다르다(`06` §7). 플로우 매칭 계열을 schnell만으로 재현하면 "소수 스텝의 흔적"만 배우게 된다.

→ **대안: SD3.5-medium(Stability Community License, 학술·비상업 무료)을 full-step 플로우 매칭 소스로 추가한다.** 이용조건을 제출자료에 명시한다. dev를 굳이 쓰려면 **생성 이미지의 제출 의무 범위를 9/18에 먼저 확인**한다.

### 2.2 OpenRAIL-M 계열의 사용제한 조항

SD1.5/SDXL의 VAE는 CreativeML OpenRAIL-M 계열이다. **재배포는 허용되나 사용제한 조항이 함께 따라간다.** p.10의 이용권과 충돌하지는 않지만, `THIRD_PARTY_NOTICES.md`에 조항이 승계된다는 사실을 명시해야 한다.

가장 깨끗한 길은 **A1의 주 VAE를 Apache-2.0(FLUX schnell VAE)으로 두고 OpenRAIL 계열은 보조로** 쓰는 것이다.

### 2.3 축 B의 라이선스 — 고전 방법이 오히려 안전하다

잎맥 분할에 공개 사전학습 모델을 가져다 쓰면 라이선스를 확인해야 하는데, **식물 표현형 분야의 공개 코드는 라이선스가 불명확한 경우가 많다.** 그런데 우리에겐 라이선스 문제 자체가 없는 길이 있다.

| 방법 | 라이선스 | 비고 |
|---|---|---|
| **Frangi / ridge 필터 등 고전 혈관강조 기법** | **없음 (알고리즘)** | scikit-image·OpenCV로 구현. **1순위** |
| 자체 학습 U-Net | 자체 | AI Hub 실제 이미지로 학습 |
| 공개 잎맥 분할 모델 | **확인 필요** | 라이선스 불명이면 사용하지 않는다 |

**B2(시설 구조물)는 Hough 변환·FFT라 애초에 라이선스 대상이 아니다.** 축 B 전체를 라이선스 리스크 없이 구성할 수 있다.

### 2.4 F6 — 제출 후에는 브랜치를 뺄 수 없다 (절차 문제)

공고문 p.10: *"제출기한 이후에는 (…) 제출자료를 수정할 수 없으며"*, *"기술적 보완은 동일한 예측결과의 재현을 위한 실행환경·경로·설정 등의 수정에 한하며, 모델 구조·가중치·예측결과 등 성능에 영향을 미치는 변경은 허용하지 않음"*

**즉 `02` §4.6의 단계적 축소(A3 → A2 → B3)를 제출 후에 적용할 수 없다.** 지표를 빼는 것은 모델 구조 변경이고 예측결과가 바뀐다.

**조치**
- 자원 초과 여부를 **제출 전에** 판정하고, **한도 안에서 도는 구성으로 확정해 제출**한다
- 축소 순서는 "사후 대응책"이 아니라 **제출 전 의사결정 순서**로 문서에 기재한다
- 제출 직전 체크리스트에 **"Colab 무료 등급에서 테스트 규모 추론 완주 + 소요 시간 측정"** 을 필수 항목으로 둔다 (`01` §7에 이미 있음 — 이제 이것이 통과 조건이다)

### 2.5 나머지 규정 항목 — 문제 없음

| 항목 | 판정 | 근거 |
|---|---|---|
| 다단계 파이프라인(분할 → 지표 → 융합) | ✅ | p.4 *"모델 구조에는 별도의 제한을 두지 않음"* |
| 축 B를 실제 이미지만으로 학습 | ✅ | 외부 데이터 출처 명시(p.5)만 지키면 됨 |
| 학습 단계의 생성형 AI 사용 | ✅ | 금지 대상은 *"평가용 테스트 데이터의 예측 과정"* 에 한정(p.5, p.10) |
| 추론 시 전부 로컬 가중치 | ✅ 필수 | 네트워크 차단 상태 완주 검증(`01` §4) |
| 재현성 (축 B가 결정적인가) | ✅ | skeletonize·Hough·FFT·SAM 모두 결정적. 시드 고정만 하면 됨 |
| 계열 분류를 병렬 점수로 사용 | ✅ | 검색 기반 attribution을 배제했으므로 외부 조회 없음 |
| Ultralytics YOLO | ❌ 배제됨 | AGPL이 산출 가중치까지 적용 |

---

## 3. 조치 목록

### 즉시 반영 (설계 변경)

- [ ] **F1** `02` §4.2 A1에 **타일 단위 VAE 왕복 + top-k 풀링** 명시
- [ ] **F2** A1의 주 VAE를 **FLUX.1-schnell VAE(Apache-2.0)** 로 지정, dev 계열 배제
- [ ] **F2** `03` 유형 ④ 레시피에서 **FLUX.1-dev → schnell + SD3.5-medium** 으로 교체
- [ ] **F4** A5 보조를 **CLIP ViT-L/14 → DINOv2 ViT-B** 로 확정
- [ ] **F5** B1 잎맥 분석을 **축소 마스크에서 수행**, 이미지당 분석 잎 수 상한
- [ ] **F6** `02` §4.6 단계적 축소를 **"제출 전 의사결정 순서"** 로 문구 변경
- [ ] **2.3** B1 구현 1순위를 **고전 ridge 필터**로 지정

### 9/18 개막설명회 확인 (`03` §5에 추가)

- [ ] **제출물 총 용량 한도** — 경량 470MB가 통과하는가
- [ ] **평가환경 GPU 등급과 런타임 한도** — 무료 T4인가 상위 GPU인가
- [ ] **테스트 이미지 장수** — 추론 예산의 미지수
- [ ] **테스트 이미지 해상도** — F1의 타일 수를 결정
- [ ] **생성 데이터의 제출 의무 범위** — 학습에 쓴 생성 이미지를 원본으로 제출해야 하는가 (F2의 회색지대)

### R1에서 실측 (`05`)

- [ ] §1.5 추론 예산표의 빈칸 — Colab 무료 T4에서 지표별 장당 소요
- [ ] 원본 해상도 VAE 왕복의 실제 VRAM 사용량 (타일 크기 결정 근거)
- [ ] PlantVillage 라이선스 원문 확인 (유형 ① 소스)
